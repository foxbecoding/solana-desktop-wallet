import {Palette} from "std-widgets.slint";

export global Theme  {
    out property<brush> primary: #2979FF;
    out property<brush> accent: #9C27B0;
    out property<brush> secondary: Palette.foreground.with-alpha(0.9);
}