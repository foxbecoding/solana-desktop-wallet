export enum View {
    Wallet,
    Collections,
    Swap,
    Explore,
    Settings,
    Accounts
}

export global ViewManager {
    in-out property <View> active_view: View.Wallet;
    pure callback cache_active_view(View);
}