import {HorizontalBox, Palette } from "std-widgets.slint";

export component Explore inherits HorizontalLayout {
    HorizontalBox {
        padding: 30px;
        Text {
            text: "Explore";
            font-size: 30px;
            font-weight: 800;
            color: Palette.foreground.with-alpha(0.85);
        }
    }
}