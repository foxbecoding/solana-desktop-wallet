import {HorizontalBox, Palette} from "std-widgets.slint";

export component Swap inherits HorizontalLayout {
    HorizontalBox {
        padding: 30px;
        Text {
            text: "Swap";
            font-size: 30px;
            font-weight: 800;
            color: Palette.foreground.with-alpha(0.85);
        }
    }
}