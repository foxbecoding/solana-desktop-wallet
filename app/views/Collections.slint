import {HorizontalBox, Palette} from "std-widgets.slint";

export component Collections inherits HorizontalLayout {
    HorizontalBox {
        padding: 30px;
        Text {
            text: "Collections";
            font-size: 30px;
            font-weight: 800;
            color: Palette.foreground.with-alpha(0.85);
        }
    }
}