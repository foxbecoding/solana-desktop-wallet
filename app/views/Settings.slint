import {HorizontalBox, Palette} from "std-widgets.slint";

export component Settings inherits HorizontalLayout {
    HorizontalBox {
        padding: 30px;
        Text {
            text: "Settings";
            font-size: 30px;
            font-weight: 800;
            color: Palette.foreground.with-alpha(0.85);
        }
    }
}