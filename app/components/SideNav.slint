import {Palette, Button, VerticalBox } from "std-widgets.slint";

export component SideNav inherits Rectangle {
    background: Palette.background;
    width: 320px;

    VerticalBox {
            Text {text: "Side Navigation";}
        }
}