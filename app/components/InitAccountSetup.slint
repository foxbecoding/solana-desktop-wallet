export global InitAccountGlobals {
    in-out property <bool> is_init_account: true;
}

export component InitAccountSetup inherits VerticalLayout {
    Text {text: "Shit goes here"; font-size: 21px; font-weight: 800;}
}