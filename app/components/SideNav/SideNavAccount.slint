import {Palette, VerticalBox } from "std-widgets.slint";

export component SideNavAccount inherits VerticalBox {
    VerticalLayout {
        Text {text: "Connected Account";}
    }
    VerticalLayout {
        Text {text: "D504F...1111";}
    }
}