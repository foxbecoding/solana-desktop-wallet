import {Palette, VerticalBox, Button} from "std-widgets.slint";
import {Account, AccountManager} from "../../utils/account-manager.slint";

export component SideNavAccount {
    height: 120px;
    private property <bool> is_accounts_set: AccountManager.accounts.length > 0 ? true : false;

    VerticalBox {
        spacing: 3px;
        visible: is_accounts_set;
        height: 100px;
        Text {
            font-size: 15px;
            font-weight: 700;
            text: AccountManager.selected_account.name;
        }
        Text {
            text: AccountManager.selected_account.pubkey_display;
            font-weight: 500;
        }
        VerticalLayout {
            visible: is_accounts_set;
            padding-top: 9px;
            Button { text: "Change Account"; }
        }
    }

    VerticalLayout {
        spacing: 6px;
        visible: !is_accounts_set;
        Rectangle {
            background: @linear-gradient(60deg, #2979FF 0%, #9C27B0 100%);
            border-radius: 6px;
            height: 120px;
            VerticalBox {
                Text {
                    visible: true;
                    font-size: 21px;
                    font-weight: 800;
                    text: "Create a account";
                    color: Palette.foreground.with-alpha(0.85);
                }
                Text {
                    visible: true;
                    font-size: 21px;
                    font-weight: 800;
                    text: "to get started";
                    color: Palette.foreground.with-alpha(0.85);
                }
                Button { text: "Create account"; }
            }
        }

    }

}