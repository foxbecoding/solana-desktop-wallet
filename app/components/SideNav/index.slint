import {Palette, VerticalBox } from "std-widgets.slint";
import { SideNavAccount } from "components.slint";

export component SideNav inherits Rectangle {
    background: Palette.background;
    width: 300px;

    SideNavAccount {}
}