import {Theme} from "../../theme.slint";

export component SideNavSolValue {
    height: 50px;

    Rectangle {
        background: Theme.surface;
        border-radius: 20px;
        height: 40px;
    }
}
