import {Palette} from "std-widgets.slint";
import {AppView} from "components/AppView.slint";
import {InitAccountSetup, InitAccountGlobals} from "components/InitAccountSetup.slint";
import {Account, AccountManager} from "utils/account-manager.slint";
import {AppManager} from "utils/app-manager.slint";

export component App inherits Window {
    background: Palette.alternate-background.darker(0.25);
    preferred-width: 900px;
    preferred-height: 600px;
    no-frame: false;
    always-on-top: false;
    title: "Solana Desktop Wallet";

    if !InitAccountGlobals.is_init_account : AppView {}
    if InitAccountGlobals.is_init_account : InitAccountSetup {}
}

export {Account, AccountManager}